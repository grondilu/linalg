use Test;
use LinAlg;

my $a = [ rand xx 3 ];
my $b = [ rand xx 3 ];

is $a + $b, [ $a Z+ $b ], "Vector addition";
is $a * $b*, ([+] $a Z* $b».conj), "scalar product";

say .perl given [[1, 2],[3, 4]] + [[0, 1],[1, 0]];
# vim: ft=perl6
